# Текущее состояние MVP

Проект реализован как компактный in-memory прототип на стандартной библиотеке Python.

## Структура

```text
C:/Users/User/lol/
  app.py
  README.md
  static/
    index.html
    app.js
    styles.css
  case/
    1_RCB/
    2_Agreements/
    3_StateTask/
    4_BUAU_Export/
  tests/
```

## Источники

- РЧБ
- Соглашения
- ГЗ: контракты
- ГЗ: платежи
- БУАУ

Проверенный объем загрузки: 4037 записей.

## Метрики

- `limit` - лимиты
- `obligation` - бюджетные обязательства
- `cash` - кассовые выбытия
- `agreement` - соглашения
- `contract` - контракты
- `payment` - платежи
- `buau` - выплаты БУ/АУ

## Поведение

Пользовательский сценарий: фильтр, выбор шаблона, выбор метрик, KPI, график, сводная таблица, детализация, trace и CSV-экспорт. Для сравнения периодов используется строгое правило `snapshot == выбранная дата`.

## Известные ограничения

- нет БД и миграций;
- нет фонового импорта;
- нет промышленной модели ошибок загрузки;
- trace для записей добавлен на уровне normalized record, но не во всех загрузчиках есть путь к исходному CSV и номер физической строки;
- парсинг сумм не использует `Decimal`.
